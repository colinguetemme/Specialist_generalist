#include <stdio.h>
#include <stdlib.h>
#include <math.h>


int main(int argc, char *argv[])
{
    int rate = 5;
    int rate_square = 0;
    rate_square = rate * rate;
    ("%d fois %d egale %d \n", rate, rate, rate*rate);
    printf("Hello world!\n");
    int compteur;
    printf("%f \n \n", sqrt(rate));
    for (compteur = 0 ; compteur < 10 ; compteur++)
    {
        printf("Hello !\n");
    }
    return 0;
}
